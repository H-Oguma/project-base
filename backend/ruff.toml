[lint]
select = ["E", "F", "B", "I", "UP", "S", "ARG", "RUF"]
ignore = []

[lint.flake8-bugbear]
# FastAPIのDepends等をデフォルト引数で使用するのを許可
extend-immutable-calls = ["fastapi.Depends", "fastapi.Query", "fastapi.Path", "fastapi.Body"]

[lint.per-file-ignores]
"test_*.py" = ["S101"]
